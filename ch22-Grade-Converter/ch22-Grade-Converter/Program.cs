using System;

namespace ch22_Grade_Converter {
    class Program {
        static void Main(string[] args) {

            Console.WriteLine("Welcome to the grade converter App!);
            string 
            while (choice.Contains("y") || choice.Contains("Y")) {
                Console.Write("Numeric Grade?: ");
                int numericGrade = int.Parse(Console.ReadLine());
                string letterGrade = "";
                if (numericGrade >= 90) {
                    letterGrade = "A";
                } else if (numbericGrade >= 88) {
                    letterGrade = "B";
                } else if (numbericGrade >= 80) {
                    letterGrade = "C";
                } else if (numbericGrade >= 67) {
                    letterGrade = "D";
                } else if (numbericGrade >= 60) {
                    letterGrade = "F"
                }
                Console.WriteLine
                    consolewrite
                    letter

                    Console.WriteLine("you entered: " + numbericGrade);
                    Console.Write("Continue?: ")
                      choice = Console.ReadLine();
                }



        }
    }
}
